# planets-app-react-native

## # Steps
Comandos necessários para rodar o App
- yarn ou npm install
- cd ios && pod install (caso use um simulador ios)
- npx react-native run-android (simulador android)
- npx react-native run-ios (simulador ios)
